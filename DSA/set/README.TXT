This replicated the Java Set interface

A set is essentially a type of list that can not cantain duplicate entries. This applies to any sort of object; 
if objectA.equals(objectB) results in returning true, Object A will not be added to the set. 
